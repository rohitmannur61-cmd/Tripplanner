"""
Project package init.

Note: Django 6.x requires the `mysqlclient` driver for the built-in MySQL backend.
If you're using MySQL Connector/Python instead, configure `MYSQL_ENGINE=connector`
in settings and install `mysql-connector-python`.
"""
